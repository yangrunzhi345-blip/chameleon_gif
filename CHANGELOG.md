# 更新日志

本文档记录 Chameleon Gif 各版本的变更(格式遵循 Keep a Changelog;版本号遵循 docs/15-发布计划.md §15.1 语义化版本)。

## [0.2.0] - 2026-08-07

### 新增

- **相机拍摄**:调用设备相机拍摄短视频 → 自动进入 GIF 工作流;Android camera 插件(CameraX),桌面 ffmpeg v4l2/dshow 采集并支持实时预览;设置页按设备能力调整相机参数
- **屏幕录制**:全屏/框选区域录制 → 产物 MP4 自动导入;Android MediaProjection 原生通道,桌面 ffmpeg x11grab/gdigrab(Wayland 自动切换 wf-recorder)
- 采集素材持久落盘(桌面 capturesDir / Android 相册),历史记录可重转,素材被删时明确提示
- 图片模式:统一画布 + 每图不扭曲(透明 pad 居中);每张图精细控制(等比缩放倍数/宽度/高度,实时预览,随任务/历史持久化可重转复现)
- 播放速度调节(0.25x 慢放 – 4x 加速)
- 图片制作 GIF 转换实时进度面板(进度条 + 百分比 + 剩余时间 + 取消)
- 缓存自动清理 + 设置页存储管理(占用统计/清空)+ 缩略图压缩
- 导出参数:等比缩放倍数(0.5–3x 联动宽高)、宽高自定义输入、预估体积 > 50MB 提醒
- Android 转换完成自动保存 GIF 到系统相册
- 首页品牌区与全屏满铺卡片风格 UI

### 修复

- 不同尺寸图片混排被强制拉伸变形(统一画布 + contain + 透明 pad)
- 100 张大图(2048×2048)转换闪退(分段 ffv1 中间片)
- Android 路径含空格致"源文件不存在"(参数数组传参)
- 完成弹窗叠加、跨任务完成事件污染、弹窗连点防御
- moov 错误特征误判源文件损坏(收紧正则匹配)
- 非 UTF-8(GBK)文件名致有效视频误判解析失败(容错解码)
- 取消流程临时文件清理竞态、任务队列取消-启动竞态
- Wayland 录屏:slurp 框选挂起、自定义区域 -g 语法、0 帧产物损坏校验
- 时间轴选区、批量表单校验、历史异步动作兜底等 30+ 项缺陷

### 变更

- Android APK 精简至约 22MB(仅 arm64-v8a)
- 体积门禁:发布产物(APK/AppImage/deb)超 30MB 即 CI 失败
- CI:release.yml 打包工作流(AppImage + deb + APK,标签 `v*` 触发)

## [0.1.0] - 2026-08-07

### 新增

- 首个版本:MVP 全链路 —— MP4 导入、实时预览、时间轴选区、参数调节(帧率/宽高/起止/循环/质量)、两遍调色板高质量导出、进度与取消
- 任务队列(双并发、失败退避重试、崩溃恢复)、历史记录(详情/重转/删除/清空)
- 多图片合成 GIF(每图停留时长/帧率/循环/质量)
- 批量导入与批量完成弹窗、设置页(主题/默认参数)
- 三平台支持:Linux(AppImage + deb)、Windows(预留打包)、Android(APK,内置 FFmpegKit)
